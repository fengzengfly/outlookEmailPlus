# TODO — Issue #52 前端邮件列表顺序修复（当前优先）

> 关联 BUG：`docs/BUG/2026-04-22-Issue52-邮件列表倒序与验证码提取失败分析.md`
> 关联 Issue：GitHub Issue #52
> 优先级：P1（前端可用性）
> 创建时间：2026-04-22
> 当前阶段：分批全量回归已完成（全部通过），进入 PR 说明整理

---

## 任务总览

| 序号 | 任务 | 优先级 | 文件 | 预计工作量 |
|------|------|--------|------|-----------|
| 1 | 明确前端顺序口径与边界 | P0 | `static/js/features/emails.js`、`static/js/main.js` | 小 |
| 2 | 设计并实现排序 helper | P0 | `emails.js`（必要时复用到 `main.js`） | 小 |
| 3 | 在关键路径接入兜底排序 | P0 | `emails.js`、`main.js` | 中 |
| 4 | 缓存/分页/选中态回归检查 | P0 | `emails.js` | 中 |
| 5 | 前端契约测试补充 | P1 | `tests/*frontend*contract*.py` 或新增测试 | 中 |
| 6 | 全量回归测试 | P0 | N/A | 中（~5 分钟） |
| 7 | 文档回写 + WORKSPACE 记录 | P1 | `docs/BUG/...`、`WORKSPACE.md` | 小 |

---

## 设计边界（本轮）

1. 本轮以**前端顺序修复**为主，不做后端提取链路根因改造。
2. 排序仅用于显示层，不修改后端分页协议（`skip/top/has_more`）。
3. 现有交互（选中态、active、加载更多、缓存恢复）必须保持。

---

## 任务详情

### Task 1：明确顺序口径

**目标**：统一“前端展示层最新优先”定义。

- [ ] 定义排序字段优先级（如 `date` -> 可解析时间戳）
- [ ] 定义不可解析时间的稳定回退策略（避免抖动）

**实现口径（已定）**：

- 时间字段读取顺序：`receivedDateTime` → `date` → `created_at` → `received_at`
- 解析方式：`Date.parse()`；不可解析则视为 `-Infinity`
- 排序规则：时间戳降序；时间相同按原始索引升序（稳定排序）

---

### Task 2：实现排序 helper

**目标**：在前端建立单一排序函数。

- [x] 在 `static/js/features/emails.js` 增加 helper（如 `sortEmailsByNewestFirst`）
- [x] 保证 helper 对空值/异常值安全

**细化**：

- [x] 增加 `resolveEmailSortTimestamp(email)`
- [x] 增加 `sortEmailsByNewestFirst(list)`

---

### Task 3：关键路径接入

**目标**：在所有主要渲染路径统一走排序 helper。

- [x] `loadEmails()` 首次加载后排序
- [x] 缓存恢复时排序
- [x] `loadMoreEmails()` 拼接后排序（保持分页语义）

**细化**：

- [x] `loadEmails` 成功后：`currentEmails` 统一赋值为排序结果
- [x] `emailListCache` 命中后：渲染前统一排序
- [x] `loadMoreEmails`：`concat` 后排序并统一 `renderEmailList`

---

### Task 4：回归检查

**目标**：确保排序接入不破坏现有体验。

- [x] 选中邮件复选框行为保持（契约回归通过）
- [x] active 详情项行为保持（契约回归通过）
- [x] “加载更多”行为与 `has_more` 状态保持（分批回归通过）

---

### Task 5：测试补充

- [x] 增加前端契约测试，至少覆盖：
  - 排序 helper 存在
  - 关键加载路径调用排序 helper

**建议测试文件**：

- `tests/test_v190_frontend_contract.py`（新增断言）
- 或新增 `tests/test_issue52_frontend_order_contract.py`

---

### Task 6：全量回归

```bash
python -m unittest discover -s tests -v
```

- [x] 无 FAIL/ERROR（分批执行完成）

---

### Task 7：文档回写

- [x] BUG 文档更新状态与最终方案
- [x] WORKSPACE 记录改动文件和测试结果
- [ ] 准备 PR 说明（前端顺序修复口径）

---

## 当前状态

| 任务 | 状态 |
|------|------|
| Task 1 顺序口径 | ✅ 文档层已完成 |
| Task 2 helper 实现 | ✅ 已完成 |
| Task 3 路径接入 | ✅ 已完成 |
| Task 4 回归检查 | ✅ 已完成（分批回归通过） |
| Task 5 测试补充 | ✅ 已完成（v190 契约测试新增） |
| Task 6 全量回归 | ✅ 已完成（6批+补充模块） |
| Task 7 文档回写 | ✅ 已完成（待补PR说明） |

---

## 风险与注意事项

1. 仅前端修复可快速稳定 UI 顺序，但不应擅自扩展到后端大改。
2. 排序接入点必须一致，否则会出现“缓存页与实时页顺序不一致”。
3. 需重点验证“加载更多后顺序 + active 高亮”是否被打乱。
